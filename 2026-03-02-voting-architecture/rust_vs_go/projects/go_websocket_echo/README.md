# Go WebSocket Echo Server

This project is a simple WebSocket echo server written in Go.

## How to run

1. Navigate to the `projects/go_websocket_echo` directory.
2. Run the following command:
   ```bash
   go run main.go
   ```
3. The server will start on `localhost:8081`.
