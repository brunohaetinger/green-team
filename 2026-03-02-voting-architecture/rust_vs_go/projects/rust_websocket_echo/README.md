# Rust WebSocket Echo Server

This project is a simple WebSocket echo server written in Rust.

## How to run

1. Navigate to the `projects/rust_websocket_echo` directory.
2. Run the following command:
   ```bash
   cargo run
   ```
3. The server will start on `127.0.0.1:8080`.
