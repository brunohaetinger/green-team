services:
  reports-dashboard:
    build:
      context: .
    container_name: reports-dashboard
    restart: on-failure
    ports:
      - "3000:80"
    networks:
      - data-kata-net

networks:
  data-kata-net:
    external: true
