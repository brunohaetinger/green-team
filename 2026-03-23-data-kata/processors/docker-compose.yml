services:
  sales-cdc-processor:
    build:
      context: ./java-cdc-kafka-processor
      dockerfile: Dockerfile
    environment:
      SPRING_KAFKA_BOOTSTRAP_SERVERS: ${KAFKA_BOOTSTRAP_SERVERS:-localhost:9092}
    restart: on-failure
